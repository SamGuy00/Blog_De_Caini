<!DOCTYPE html>
<html>
    <head>
        <title>Rase de caini</title>
        <meta charset="UTF-8"/>
        <link rel="stylesheet" href="style.css"/>
        <script src="index.js" defer></script>
    </head>
    <body>
        <div class="container">
            <div class="title">Choose your favorite dog</div>
            <div class="items">
                <%
                result.forEach((item) => {
                    %>
                    <div class="item" onclick="showPupUp('<%=item.name%>', `<%=item.description%>`)">
                        <img src="<%=item.image%>" alt="dog"/>
                        <p><%=item.name%></p>
                    </div>
                    <%
                })
                %>
            </div>
        </div>
        <div class="popup">
            <h2></h2>
            <a class="close" href="#" onclick="hidePopUp()">&times;</a>
            <div class="content"></div>
        </div>
    </body>
</html>